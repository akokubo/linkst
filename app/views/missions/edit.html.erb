<% provide :title,  t("Editing ", model: t('activerecord.models.mission').pluralize(:locale)) %>
<h1><%= yield(:title) %></h1>

<%= render 'form' %>

<%= link_to t('Show'), @mission %> |
<%= link_to t('Back'), missions_path %>
