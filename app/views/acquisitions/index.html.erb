<h1>Listing acquisitions</h1>

<table>
  <thead>
    <tr>
      <th>Mission</th>
      <th>Category</th>
      <th>Experiences</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @acquisitions.each do |acquisition| %>
      <tr>
        <td><%= acquisition.mission %></td>
        <td><%= acquisition.category %></td>
        <td><%= acquisition.experiences %></td>
        <td><%= link_to 'Show', acquisition %></td>
        <td><%= link_to 'Edit', edit_acquisition_path(acquisition) %></td>
        <td><%= link_to 'Destroy', acquisition, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Acquisition', new_acquisition_path %>
